import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import ModalWrapper from '@/components/ModalWrapper'

const IncomeModal = () => {
    return (
        <ModalWrapper>
            <Text>IncomeModal</Text>
        </ModalWrapper>
    )
}

export default IncomeModal

const styles = StyleSheet.create({})